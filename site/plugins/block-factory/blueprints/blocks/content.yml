name: Content
icon: text
tabs:
  main:
    label: Block Content
    fields:

      headline:
        type: headline
        label: Add flexible content.
        help: Build your own content cluster using headers, text blocks, buttons, and CTAs. Or just one of those things. It's up to you.
      
      content:
        label: Content
        type: blocks
        fieldsets:
          - headline
          - subheadline
          - copy
          - button
          - cta

  content-settings:
    label: Content Settings
    extends: tabs/content-settings
  
  block-settings:
    label: Block Settings
    extends: tabs/block-settings