name: Content
icon: text
tabs:
  main:
    label: Block Content
    fields:
      content:
        label: Content
        type: blocks
        help: Build your own content group using headers, text blocks, buttons, and CTAs.
        fieldsets:
          - headline
          - subheadline
          - copy
          - button
          - cta

  content-settings:
    label: Content Settings
    extends: tabs/content-settings
  
  block-settings:
    label: Block Settings
    extends: tabs/block-settings

preview: fields